<h1 class="display-4">Word changes requested by users</h1>
<p class="lead">Check the box next to any change that you want to apply to the database. When you click 'Process', any unchecked change will be discarded so don't proceed until you're ready to do them all.</p>
<hr>
<form method="post">
    {{#if addRequests}}
    <h2 class="display-5">Additions</h2>
        <ul class="list-group">
        {{#each addRequests}}
            <li class="list-group-item">
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" name="add:{{this}}" value="true">
                    <label class="form-check-label" for="exampleCheck1">{{this}}</label>
                </div>
            </li>
        {{/each}}
        </ul>
    {{/if}}
    {{#if removeRequests}}
    <h2 class="display-5">Removals</h2>
        <ul class="list-group">
        {{#each removeRequests}}
            <li class="list-group-item">
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" name="remove:{{this}}" value="true">
                    <label class="form-check-label" for="exampleCheck1">{{this}}</label>
                </div>
            </li>
        {{/each}}
        </ul>
    {{/if}}
    {{#if addRequests}}
    <button type="submit" formaction="/verified/processChanges" value="Process" class="btn btn-primary">Process</button>
    {{else if removeRequests}}
    <button type="submit" formaction="/verified/processChanges" value="Process" class="btn btn-primary">Process</button>
    {{/if}}
</form>
